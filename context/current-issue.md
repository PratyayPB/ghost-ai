Context:There are different shapes like rectangle, diamond, circle , pill etc in the bottom navbar. We can drag the shapes from the navbar and when we drop it on the canvas, it creates an element of the shape that the user dragged from the bottom navbar

Issue: No matter whatever shape the user drags, when the user drops it on the canvas, it always forms a rectangle shape. Example: If a user drags a diamond from the navbar and drops it in the canvas, a rectangle element is created on the canvas

Fix the issue.
Make no other changes
